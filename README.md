 full devops project deployment on kubernetes via argocd 
 steps to run  this project
 steps:
 1: clone repo from server git clone
 2: install reqired tools
    tools:
        jenkins
        helm 
        minkube
        argocd in minikube namespace argocd  vai helm repo 
        promql stack install via helm in namespace monitoring 
        docker 
        git 

 3: ensure all running component
 4: run jenkins job 
 5: job successful then see in browser app 
